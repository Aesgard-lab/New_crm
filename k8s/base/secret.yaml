apiVersion: v1
kind: Secret
metadata:
  name: crm-secrets
  namespace: crm
type: Opaque
stringData:
  # IMPORTANTE: En produccion usar sealed-secrets o external-secrets
  # Estos valores son placeholders - reemplazar antes de deploy
  DJANGO_SECRET_KEY: "CHANGE-ME-IN-PRODUCTION-use-sealed-secrets"
  POSTGRES_USER: "crm_user"
  POSTGRES_PASSWORD: "CHANGE-ME-IN-PRODUCTION"
  REDIS_PASSWORD: "CHANGE-ME-IN-PRODUCTION"
  SENTRY_DSN: ""
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
  EMAIL_HOST_PASSWORD: ""
